global:
  postgresql:
    auth:
      username: appeal
      database: appeal
      existingSecret: appeal-db-secrets
      secretKeys:
        userPasswordKey: psqluserpass
        adminPasswordKey: psqladminpass